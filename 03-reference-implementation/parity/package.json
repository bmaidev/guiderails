{
  "name": "@guiderails/parity-audit",
  "version": "0.1.0",
  "description": "Automated parity checks between the baseline and conformant fixture builds. Anything that differs must be a declared divergence; anything else is a parity defect. The audit report is signed by an auditor who built neither build.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"src/**/*.test.ts\"",
    "report": "node src/report.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
